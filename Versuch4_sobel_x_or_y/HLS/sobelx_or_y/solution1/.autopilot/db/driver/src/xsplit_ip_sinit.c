// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.2
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xsplit_ip.h"

extern XSplit_ip_Config XSplit_ip_ConfigTable[];

XSplit_ip_Config *XSplit_ip_LookupConfig(u16 DeviceId) {
	XSplit_ip_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XSPLIT_IP_NUM_INSTANCES; Index++) {
		if (XSplit_ip_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XSplit_ip_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XSplit_ip_Initialize(XSplit_ip *InstancePtr, u16 DeviceId) {
	XSplit_ip_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XSplit_ip_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XSplit_ip_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

